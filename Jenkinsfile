node {  
    agnet any
    // tools {

    // }
    stage("Build") { 
        steps{
          echo 'building the application '
        }
    }
    stage("Test") { 
      when {
        expression{
          
        }
      }
        steps{
          echo 'Testing the application '
        } 
    }
    stage("Deploy") { 
        steps{
          echo 'Deploying the application '
        } 
    }
    posts{
    
    always {
    //send email 
    //docker-compose down
    }
    
    
    success {
    //when sucess 
    }
    
    
    failure {
    //when failed
    }
    
    }
}
